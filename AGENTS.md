## 硬件场景

机架:Mark5 五寸机

电机:2207尺寸的1950KV电机

陀螺仪:ICM42688

TOF: VL53L1X 两个 分别安装在2号机臂 3号机臂

光流模块:PMW3901

气压计:BMP388

电池:4S航模电池

使用地面电缆远程供电(会给飞机外力,会偶然遮挡住TOF)

目标: 根据以上的硬件条件,实现家庭环境内的(室内无GPS)(光照条件不足) 稳定的1米至1.5米的稳定定高定点

## 机体坐标系极性规范

- 坐标轴（机体系 FRD）：`+X` 前，`+Y` 右，`+Z` 下
- 姿态角：`roll > 0` 右倾；`pitch > 0` 抬头（前倾 `pitch < 0`）；`yaw > 0` 俯视顺时针
- 角速度（陀螺仪）：`gx > 0` 右滚；`gy > 0` 抬头；`gz > 0` 俯视顺时针
- 加速度计（比力）：`+ax` 向前加速；`+ay` 向右加速；`+az` 向下加速；静止平放：`ax≈0, ay≈0, az≈-1g`
- 光流：向右水平移动 `Δx > 0`；向前水平移动 `Δy > 0`
- 电机编号：`1=右下`，`2=右上`，`3=左下`，`4=左上`
- 电机旋向：`1/4` 顺时针（CW），`2/3` 逆时针（CCW）
