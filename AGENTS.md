## 硬件场景

机架:Mark5 五寸机

电机:2207尺寸的1950KV电机

陀螺仪:ICM42688

TOF: VL53L1X 四个 分别安装在每个机架下面

光流模块:PMW3901

电池:4S航模电池

使用地面电缆远程供电(会给飞机外力,会偶然遮挡住TOF)

目标: 根据以上的硬件条件,实现家庭环境内的(室内无GPS)(光照条件不足) 稳定的1米至1.5米的稳定定高定点

需要知道的是，对于五寸机以及这款电机来说，是适用于穿越机百公里时速飞行的，所以对于我当前室内1米到5米之间高度的稳定飞行来说，是比较平滑的

## 机体坐标系极性规范

- 坐标轴（机体系 FRD）：`+X` 前，`+Y` 右，`+Z` 下
- 姿态角：`roll > 0` 右倾；`pitch > 0` 抬头（前倾 `pitch < 0`）；`yaw > 0` 俯视顺时针
- 角速度（陀螺仪）：`gx > 0` 右滚；`gy > 0` 抬头；`gz > 0` 俯视顺时针
- 加速度计（比力）：`+ax` 向前加速；`+ay` 向右加速；`+az` 向下加速；静止平放：`ax≈0, ay≈0, az≈-1g`
- 光流：

  /* ======================== 光流极性映射 ======================== */

  #definePMW3901_SIGN_X           (1)                 /* X轴极性：芯片X方向与机体X方向同向 */

  #definePMW3901_SIGN_Y           (1)                /* Y轴极性：芯片Y方向与机体Y方向反向 */
  基于以上的情况

  向左水平移动->g_pmw3901_raw.deltaX>0

  向右水平移动->g_pmw3901_raw.deltaX<0

  向前水平移动->g_pmw3901_raw.deltaY>0

  向右水平移动->g_pmw3901_raw.deltaY<0
- 
- 电机编号：`1=右下`，`2=右上`，`3=左下`，`4=左上`
- 电机旋向：`1/4` 顺时针（CW），`2/3` 逆时针（CCW）

## 中文注释规范要求

1. **宏定义**
   * 所有宏定义必须添加中文注释。
   * 注释要求简短、明确，准确说明宏的用途或含义。
2. **全局变量**
   * 所有全局变量必须添加中文注释。
   * 注释需说明变量的作用、单位（如有）、使用场景等，要求简洁清晰。
3. **函数注释**
   * 函数**声明和定义**都必须添加完整的中文注释。
   * 注释内容必须包含：
     * 函数功能说明
     * 输入参数说明（名称、含义、单位/范围如有）
     * 输出参数或返回值说明（含义及状态说明）
   * 要求描述准确，使读者无需阅读函数体即可理解函数用途。
4. **函数内部注释**
   * 当多条语句共同完成一个功能时，应在该功能代码段前添加 **功能性注释** 。
   * 注释需概括说明该代码段实现的整体功能。
   * 不需要对每一条语句单独注释。
   * 要求读者通过阅读注释即可快速理解下面代码块的目的。

## AI代码修改注意

1. 不要尝试更改文件原本的编码格式!!!哪怕在你的视角是乱码,那就不修改!!!只修改英文关键代码
2. 每次修改保持最简单的原则,不要生成很多的杂七杂八的函数,那些五个语句能够解决的,就没必要封装为一个函数
3. 修改完之后,不需要尝试从命令行调用iar对项目进行编译,我会编译完并且把结果告诉你
